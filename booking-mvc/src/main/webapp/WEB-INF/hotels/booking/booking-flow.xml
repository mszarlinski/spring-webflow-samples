<?xml version="1.0" encoding="UTF-8"?>
<flow xmlns="http://www.springframework.org/schema/webflow"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
      	http://www.springframework.org/schema/webflow
      	http://www.springframework.org/schema/webflow/spring-webflow-2.0.xsd">

	<secured attributes="ROLE_USER" />

	<input name="hotelId" required="true" />

	<on-start>
		<evaluate expression="bookingService.createBooking(hotelId, currentUser.name)" result="flowScope.booking" />
	</on-start>
	
	<view-state id="enterBookingDetails" model="booking">
		<binder>
			<binding property="checkinDate" />
			<binding property="checkoutDate" />
			<binding property="beds" />
			<binding property="smoking" />
			<binding property="creditCard.number" />
			<binding property="creditCard.name" />
			<binding property="creditCard.expiryMonth" />
			<binding property="creditCard.expiryYear" />
			<binding property="amenities" />
		</binder>
		<on-render>
			<render fragments="body" />
		</on-render>
		<transition on="proceed" to="reviewBooking">
			<set name="booking.dirty" value="true"/>
		</transition>
		<transition on="cancel" to="cancel" bind="false" />
	</view-state>
	
	<view-state id="reviewBooking" model="booking">
		<on-render>
			<render fragments="body" />
		</on-render>
		<transition on="CONFIRMED" to="bookingConfirmed"/>
		<transition on="REVISED" to="enterBookingDetails" />
		<transition on="CANCELLED" to="cancel" />
	</view-state>
	
	<end-state id="bookingConfirmed">
		<output name="confirmed" value="'Your booking is confirmed, you can book another hotel by searching again.'"/>
	</end-state>

	<end-state id="cancel" />

</flow>
